# config.js IS committed in this repo — the Supabase publishable key is
# safe to publish (Row Level Security enforces what each user can see).

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
