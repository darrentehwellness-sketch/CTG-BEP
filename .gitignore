# Local-only configuration (real Supabase keys go here — anon key is safe to commit,
# but we leave config.js out so each environment can have its own).
# If you want the same keys for everyone, delete this line and commit config.js.
config.js

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
